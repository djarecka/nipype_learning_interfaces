machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "~/nipype_data"
  override:
    - docker pull nipype/workshops:latest-base

test:
  override:
    - docker run -it --rm -v $PWD:/home/jovyan/work nipype/workshops:latest-base pytest tests_unit/test_temp.py

