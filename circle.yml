machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "~/nipype_data"
  override:
    - docker pull nipype/workshops:latest-complete

test:
  override:
    - docker run -it --rm -v $PWD:/home/jovyan/work nipype/workshops:latest-complete pytest tests_unit/

